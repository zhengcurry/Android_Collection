<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.curry.android.android_collection">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.TestImmersionActivity"
            android:theme="@style/TranslucentTheme"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.chart.LineChartActivity"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".activity.chart.BarChartActivity"></activity>
        <activity android:name=".activity.chart.CombinedChartActivity"></activity>
        <activity android:name=".activity.CanvasActivity"></activity>
    </application>

</manifest>